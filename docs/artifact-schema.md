# Artifact Schema

Every major artifact should have:

- id
- title
- artifact_type
- project
- path or storage target
- optional metadata
- optional body content

Artifacts do not replace raw conversations.
Raw conversation is always saved first.
